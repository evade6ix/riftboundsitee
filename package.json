{
  "name": "riftbounddd",
  "version": "1.0.0",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "dev": "NODE_ENV=development node server/index.js",
    "start": "NODE_ENV=production node server/index.js",
    "seed:riftbound": "NODE_ENV=development node server/scripts/seedRiftbound.js",
    "postinstall": "npm --prefix client install --omit=dev=false && npm --prefix client run build"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
